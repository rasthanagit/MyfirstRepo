name: Deploy Parameter Store
on:
  workflow_dispatch:
env:
  AWS_REGION: ${{ vars.AWS_REGION }}
jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v3
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: ${{ secrets.IAM_ROLE }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Check if stack exists
        id: check_stack
        run: |
          if aws cloudformation describe-stacks --stack-name parameter-store-prod --region us-east-2 2>/dev/null; then
            echo "Stack exists"
            echo "stack_exists=true" >> $GITHUB_OUTPUT
            
            echo "Current stack parameters:"
            aws cloudformation describe-stacks \
              --stack-name parameter-store-prod \
              --region us-east-2 \
              --query 'Stacks[0].Parameters' \
              --output table
          else
            echo "Stack does not exist"
            echo "stack_exists=false" >> $GITHUB_OUTPUT
          fi
        continue-on-error: true
      
      - name: Build
        run: sam build
      
      - name: Validate template
        run: |
          echo "Validating SAM template..."
          sam validate --region us-east-2
          
          echo "Checking built template..."
          cat .aws-sam/build/template.yaml
      
      - name: Deploy
        env:
          COGNITO_CLIENT_ID: ${{ secrets.COGNITO_CLIENT_ID }}
          COGNITO_USER_POOL_ID: ${{ secrets.COGNITO_USER_POOL_ID }}
          EMAIL_FROM_EMAIL: ${{ secrets.EMAIL_FROM_EMAIL }}
          CONTRACT_PORTAL_HMAC_SECRET: ${{ secrets.CONTRACT_PORTAL_HMAC_SECRET }}
          HUBSPOT_API_KEY: ${{ secrets.HUBSPOT_API_KEY }}
          DB_CONNECTION_STRING: ${{ secrets.DB_CONNECTION_STRING }}
          SMTP_DETAILS: ${{ secrets.SMTP_DETAILS }}
          SNS_PROJECT: ${{ secrets.SNS_PROJECT }}
          SNS_PUSH_NOTIFICATION: ${{ secrets.SNS_PUSH_NOTIFICATION }}
          SNOWFLAKE_USERNAME: ${{ secrets.SNOWFLAKE_USERNAME }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
        run: |
          # Strip ALL whitespace including newlines, tabs, etc from secrets
          COGNITO_CLIENT_ID=$(echo "$COGNITO_CLIENT_ID" | tr -d '[:space:]')
          COGNITO_USER_POOL_ID=$(echo "$COGNITO_USER_POOL_ID" | tr -d '[:space:]')
          EMAIL_FROM_EMAIL=$(echo "$EMAIL_FROM_EMAIL" | tr -d '[:space:]')
          CONTRACT_PORTAL_HMAC_SECRET=$(echo "$CONTRACT_PORTAL_HMAC_SECRET" | tr -d '[:space:]')
          HUBSPOT_API_KEY=$(echo "$HUBSPOT_API_KEY" | tr -d '[:space:]')
          DB_CONNECTION_STRING=$(echo "$DB_CONNECTION_STRING" | tr -d '[:space:]')
          SMTP_DETAILS=$(echo "$SMTP_DETAILS" | tr -d '[:space:]')
          SNS_PROJECT=$(echo "$SNS_PROJECT" | tr -d '[:space:]')
          SNS_PUSH_NOTIFICATION=$(echo "$SNS_PUSH_NOTIFICATION" | tr -d '[:space:]')
          SNOWFLAKE_USERNAME=$(echo "$SNOWFLAKE_USERNAME" | tr -d '[:space:]')
          SNOWFLAKE_PASSWORD=$(echo "$SNOWFLAKE_PASSWORD" | tr -d '[:space:]')
          SNOWFLAKE_WAREHOUSE=$(echo "$SNOWFLAKE_WAREHOUSE" | tr -d '[:space:]')
          
          PARAMS="ParameterKey=CognitoClientId,ParameterValue=${COGNITO_CLIENT_ID}"
          PARAMS="${PARAMS} ParameterKey=CognitoUserPoolId,ParameterValue=${COGNITO_USER_POOL_ID}"
          PARAMS="${PARAMS} ParameterKey=EmailFromEmail,ParameterValue=${EMAIL_FROM_EMAIL}"
          PARAMS="${PARAMS} ParameterKey=ContractPortalHmacSecret,ParameterValue=${CONTRACT_PORTAL_HMAC_SECRET}"
          PARAMS="${PARAMS} ParameterKey=HubspotApiKey,ParameterValue=${HUBSPOT_API_KEY}"
          PARAMS="${PARAMS} ParameterKey=DbConnectionString,ParameterValue=${DB_CONNECTION_STRING}"
          PARAMS="${PARAMS} ParameterKey=SmtpDetails,ParameterValue=${SMTP_DETAILS}"
          PARAMS="${PARAMS} ParameterKey=SnsProject,ParameterValue=${SNS_PROJECT}"
          PARAMS="${PARAMS} ParameterKey=SnsPushNotification,ParameterValue=${SNS_PUSH_NOTIFICATION}"
          PARAMS="${PARAMS} ParameterKey=SnowflakeUsername,ParameterValue=${SNOWFLAKE_USERNAME}"
          PARAMS="${PARAMS} ParameterKey=SnowflakePassword,ParameterValue=${SNOWFLAKE_PASSWORD}"
          PARAMS="${PARAMS} ParameterKey=SnowflakeWarehouse,ParameterValue=${SNOWFLAKE_WAREHOUSE}"
          
          sam deploy \
            --stack-name parameter-store-prod \
            --region us-east-2 \
            --capabilities CAPABILITY_IAM \
            --resolve-s3 \
            --no-confirm-changeset \
            --no-fail-on-empty-changeset \
            --parameter-overrides "${PARAMS}"
      
      - name: Get deployment error details
        if: failure()
        run: |
          echo "=== Fetching CloudFormation Stack Events ==="
          aws cloudformation describe-stack-events \
            --stack-name parameter-store-prod \
            --region us-east-2 \
            --max-items 50 \
            --output json | jq -r '.StackEvents[] | select(.Timestamp > (now - 600)) | "\(.Timestamp) | \(.ResourceType) | \(.ResourceStatus) | \(.ResourceStatusReason // "N/A")"'
          
          echo ""
          echo "=== Listing Change Sets ==="
          aws cloudformation list-change-sets \
            --stack-name parameter-store-prod \
            --region us-east-2 \
            --output json | jq -r '.Summaries[]? | "\(.ChangeSetName) | \(.Status) | \(.StatusReason // "N/A")"'
          
          echo ""
          echo "=== Describing Latest Failed Change Set ==="
          LATEST_CS=$(aws cloudformation list-change-sets \
            --stack-name parameter-store-prod \
            --region us-east-2 \
            --output json | jq -r '.Summaries[0].ChangeSetName // empty')
          
          if [ ! -z "$LATEST_CS" ]; then
            aws cloudformation describe-change-set \
              --change-set-name "$LATEST_CS" \
              --stack-name parameter-store-prod \
              --region us-east-2 \
              --output json | jq '.StatusReason, .Changes'
          fi
